<div class="bg-[var(--background)] text-[var(--text)] py-60 lg:py-[86px]" style="{% render "color-variables", color: section.settings.color %}">
    <div class="page-width">
        <div class="flex flex-col gap-50 justify-center items-center">
            {% if section.settings.heading != blank %}
                <h4 class="text-arizona-42 lg:text-arizona-48 text-center">
                    {{ section.settings.heading }}
                </h4>
            {% endif %}

            {% if section.blocks != blank %}
                <div class="flex flex-col lg:flex-row justify-center flex-wrap gap-y-70 items-center text-center gap-x-[max(100px,17vw)]">
                    {% for block in section.blocks %}
                        <div class="wysiwyg text-center flex-1">
                            {{ block.settings.heading }}
                        </div>
                    {% endfor %}
                </div>
            {% endif %}

            {% if section.settings.button_label != blank and section.settings.button_link != blank %}
                {% if section.settings.button_style == 'link' %}
                        <a href="{{ section.settings.button_link }}" class="inline-block text-link text-pangram-14 leading-[30px] w-fit">
                            {{ section.settings.button_label }}
                        </a>
                {% else %}
                    <div class="flex flex-col justify-center lg:justify-end items-center w-full mt-auto">
                    <a href="{{ section.settings.button_link }}" class="button button--outline button--large w-full">
                        {{ section.settings.button_label }}
                    </a>
                    </div>
                {% endif %}
            {% endif %}
        </div>
    </div>
</div>

{% schema %}
    {
        "name": "Text Columns",
        "class": "text-columns",
        "settings": [
            {
                "type": "select",
                "id": "color",
                "label": "Section colors",
                "options": [
                    {
                        "label": "Light",
                        "value": "light"
                    },
                    {
                        "label": "Dark",
                        "value": "dark"
                    }
                ],
                "default": "dark"
            },
            {
                "type": "textarea",
                "id": "heading",
                "label": "Text"
            },
            {
                "type": "header",
                "content": "Button"
            },
            {
                "type": "text",
                "id": "button_label",
                "label": "Label"
            },
            {
                "type": "url",
                "id": "button_link",
                "label": "Link",
                "info": "Leave Label field blank to make entire slide clickable"
            },
            {
                "type": "select",
                "id": "button_style",
                "label": "Style",
                "options": [
                    {"label": "Button", "value": "button"},
                    {"label": "Link", "value": "link"}
                ],
                "default": "link"
            }
        ],
        "blocks": [
            {
                "type": "column",
                "name": "Column",
                "settings": [
                    {
                        "type": "richtext",
                        "id": "heading",
                        "label": "Text"
                    }
                ]
            }
        ],
        "presets": [
            {
                "name": "Text Columns"
            }
        ]
    }
{% endschema %}